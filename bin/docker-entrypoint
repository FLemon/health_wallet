#!/bin/bash -e

# If running the Rails server then prepare the database and create Mongoid indexes.
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
  ./bin/rails db:mongoid:create_indexes
fi

exec "${@}"
